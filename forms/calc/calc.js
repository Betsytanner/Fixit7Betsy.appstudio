//works execpt for it will just print the numbers instead of the addition of them 
function Calculate(num1, num2) {
  return (`num1 + num2`);
  }

let numA = prompt("Enter one number")
let numB = prompt("Enter a 2nd  number")
let summedNumbers = Calculate(numA, numB)

console.log(alert(`The sum of ${numA} and ${numB} is ${summedNumbers}`));



